services:
  app:
    build:
      context: .
      dockerfile: Containerfile
    environment:
      # Set defaults and specify variables here for coolify to pick up
      - 'SURREAL_USER=${SERVICE_USER_APP}'
      - 'SURREAL_PASS=${SERVICE_PASSWORD_APP}'
      - 'PUBLIC_URI=${PUBLIC_URI:-https://lex.local.crux-bphc.com}'
      - 'CORS_ORIGINS=${CORS_ORIGINS:-*}'
    depends_on:
      db:
        condition: service_healthy

  db:
    extends:
      file: compose.base.yml
      service: db
    volumes:
      - database:/database.db

volumes:
  database: