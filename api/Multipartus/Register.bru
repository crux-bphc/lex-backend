meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/impartus/user
  body: json
  auth: inherit
}

body:json {
  {
    "password": "{{impartus_password}}"
  }
}

docs {
  Register the current user for multipartus on lex.
}
